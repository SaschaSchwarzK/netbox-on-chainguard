[project]
name = "netbox-on-chainguard"
version = "0.1.0"
requires-python = ">=3.14"
authors = [
    { name = "Sascha Schwarz", email = "sascha@providerhoelle.de" },
]

[dependency-groups]
dev = [
    "ruff",
    "mypy",
]

[tool.ruff]
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I"]
